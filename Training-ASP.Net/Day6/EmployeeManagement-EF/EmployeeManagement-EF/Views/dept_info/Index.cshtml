@model IEnumerable<EmployeeManagement_EF.Models.dept_info>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.d_Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.deptname_info.Department)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.d_Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.deptname_info.Department)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.d_No }) |
            @Html.ActionLink("Details", "Details", new { id=item.d_No }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.d_No })
        </td>
    </tr>
}

</table>
